{
  "name": "slack-deployment",
  "engines": {
    "node": ">=16.0.0"
  },
  "packageManager": "yarn@3.2.2",
  "devDependencies": {
    "@types/node": "^18.7.16",
    "@vercel/ncc": "^0.34.0",
    "prettier": "^2.7.1",
    "typescript": "^4.8.3"
  },
  "dependencies": {
    "@actions/core": "^1.9.1",
    "@actions/github": "^5.0.3"
  },
  "main": "src/main.ts",
  "scripts": {
    "build": "npm run build:compile && npm run build:package",
    "build:compile": "tsc",
    "build:package": "ncc build --source-map"
  }
}
